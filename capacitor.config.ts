import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kaseradtke.projectdayjump',
  appName: 'project-day-jump',
  webDir: 'dist'
};

export default config;
